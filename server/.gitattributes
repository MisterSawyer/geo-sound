# Ensure all text files use LF (important for shell scripts)
* text=auto eol=lf

# Explicitly enforce LF for shell scripts
*.sh text eol=lf

# Python files (force LF line endings)
*.py text eol=lf

# Mark shell scripts as diffable text
*.sh diff

# Ignore binary diffs for media files
*.mp3 binary
*.wav binary
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary

# Templates (Jinja2, HTML)
*.html text
*.jinja text
*.j2 text
*.css text
*.js text

# JSON / YAML configs
*.json text
*.yml  text
*.yaml text
